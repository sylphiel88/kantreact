import React from "react";

function NextButton(props){
    <input type="button" className="pagesForw" id="us" value=">" onClick={props.page === props.pages ? null : props.nextButton}></input>
}

export default NextButton